[project]
name = "agentlens-graph-engine"
version = "0.1.0"
description = "Graph data model and algorithms for multi-agent organizational graphs"
requires-python = ">=3.11"
license = "Apache-2.0"
dependencies = [
    "pydantic>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
