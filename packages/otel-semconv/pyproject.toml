[project]
name = "agentlens-otel-semconv"
version = "0.1.0"
description = "OpenTelemetry semantic conventions for multi-agent systems"
requires-python = ">=3.11"
license = "Apache-2.0"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["agentlens_otel_semconv"]
